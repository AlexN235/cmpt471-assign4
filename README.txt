CMPT 471 - Computer Network II - Assignment 4

This code takes a message and encrpyts it. Then the message is sent over an virtual network and decrypted on the other side. 

client.py - creates a socket for the client side and pushes the messages through the client side socket.

server.py - creates a socket for the server side and recieves the message sent from the client side.

EncryptTechnique.py - contains code of different encryption techniques.

MultiEncryption.py - contains code to encrypt and decrypt the message.

